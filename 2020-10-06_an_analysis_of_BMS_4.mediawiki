バシク行列システム 4 を解析する。

== 解析の手順 ==

ここでの解析は[[ユーザーブログ:Hexirp/(2020-04-04)_バシク行列システムの解析]]を元にして改善したスタイルで行う。

# \( B \) を解析する。
# 情報が足りない。
# \( B[0] = ε \) を解析する。
# \( ε \) は最小元なので、 \( 0 \) と等しい。
# \( ε \) は \( 0 \) である。
# 情報が足りない。
# \( B[1] = (0)(1) \) を解析する。
# 情報が足りない。
# \( (0)(1)[0] = (0) \) を解析する。
# \( (0) \) は \( 0 \) より大きい最小の要素なので、 \( ω ^ 0 \) と等しい。
# \( (0) \) は \( ω ^ 0 \) である。
# ……

ここで、木構造を見取ることが出来る。入れ子にしてはっきりさせると、次のようになる。

# \( B \) を解析する。
## 情報が足りない。
### \( B[0] = ε \) を解析する。
#### \( ε \) は最小元なので、 \( 0 \) と等しい。
### \( ε \) は \( 0 \) である。
## 情報が足りない。
### \( B[1] = (0)(1) \) を解析する。
#### 情報が足りない。
##### \( (0)(1)[0] = (0) \) を解析する。
###### \( (0) \) は \( 0 \) より大きい最小の要素なので、 \( ω ^ 0 \) と等しい。
##### \( (0) \) は \( ω ^ 0 \) である。
#### ……

この木構造を折り畳み要素で表現する。無駄な言葉は可能な限り省く。

== 解析 ==

表記は以下のように設定する。他の表記から容易に得られる場合は省略する。

* BMS - バシク行列システム 4
* CNF - カントール標準形
* CNF(A) - カントール標準形（略記）
* CNF+EF - カントール標準形にエプシロン表記を加えたもの
* CNF+EF(A) - カントール標準形にエプシロン表記を加えたもの（略記）
* CNF+EF(ME) - カントール標準形にエプシロン表記を加えたもの（乗算と冪乗）
* VH - ヴェブレン階層
* VH(A) - ヴェブレン階層（略記）
* VH(ME) - ヴェブレン階層（乗算と冪乗）
* MVF - 多変数ヴェブレン関数
* MVF(A) - 多変数ヴェブレン関数（略記）
* MVF(ME) - 多変数ヴェブレン関数（乗算と冪乗）
* TVF - 超限変数ヴェブレン関数
* TVF - 超限変数ヴェブレン関数（略記）
* TVF - 超限変数ヴェブレン関数（乗算と冪乗）
* BRF - ラティエンにより再構成されたバッハマン関数
* BRF(A) - ラティエンにより再構成されたバッハマン関数（略記）
* BRF - ラティエンにより再構成されたバッハマン関数（乗算と冪乗）
* BRF+VH - ラティエンにより再構成されたバッハマン関数のベースをヴェブレン階層に取り換えたもの
* BRF+VH(A) - ラティエンにより再構成されたバッハマン関数のベースをヴェブレン階層に>
取り換えたもの（略記）
* BRF+VH(ME) - ラティエンにより再構成されたバッハマン関数のベースをヴェブレン階層に取り換えたもの（乗算と冪乗）
* BF - ブーフホルツ関数
* BF(A) - ブーフホルツ関数（略記）
* BF - ブーフホルツ関数（乗算と冪乗）
* EBF - 拡張ブーフホルツ関数
* EBF(A) - 拡張ブーフホルツ関数（略記）
* JF - イェーガー関数
* RFM - マーロ基数を崩壊させるラスジェン関数

ここから解析を開始する。

{{User:Hexirp/tcu}}
\( B \)
{{User:Hexirp/tcm}}
{{User:Hexirp/tcu}}
\( B[0] = \varepsilon \)
{{User:Hexirp/tcm}}
\( \varepsilon \) は最小のものであるため、 \( 0 \) に対応する。

* CNF(A) - \( 0 \)
{{User:Hexirp/tcl}}
{{User:Hexirp/tcu}}
\( B[1] = (0)(1) \)
{{User:Hexirp/tcm}}
{{User:Hexirp/tcu}}
\( (0)(1)[0] = (0) \)
{{User:Hexirp/tcm}}
\( (0) \) は \( \varepsilon \) よりも大きい最小のものであるため、 \( 1 \) に対応する。

* CNF(A) - \( 1 \)
{{User:Hexirp/tcl}}
{{User:Hexirp/tcu}}
\( (0)(1)[1] = (0)(0) \)
{{User:Hexirp/tcm}}
\( (0)(0) \) は \( (0) \) よりも大きい最小のものであるため、 \( 2 \) に対応
する。

* CNF(A) - \( 2 \)
{{User:Hexirp/tcl}}
{{User:Hexirp/tcu}}
\( (0)(1)[2] = (0)(0)(0) \)
{{User:Hexirp/tcm}}
\( (0)(0)(0) \) は \( (0)(0) \) よりも大きい最小のものであるため、 \( 3 \) に対応
する。

* CNF(A) - \( 3 \)
{{User:Hexirp/tcl}}
{{User:Hexirp/tcu}}
\( (0)(1)[3] = (0)(0)(0)(0) \)
{{User:Hexirp/tcm}}
\( (0)(0)(0)(0) \) は \( (0)(0)(0) \) よりも大きい最小のものであるため、 \( 4 \) に対応
する。

* CNF(A) - \( 4 \)

{{User:Hexirp/bxu|法則-1}}
\( A \) に \( (0) \) を加えることは \( A \) に \( 1 \) を足すことに等しい。
{{User:Hexirp/bxl}}
{{User:Hexirp/tcl}}
<!-- 途中 -->
{{User:Hexirp/tcl}}
{{User:Hexirp/tcl}}
